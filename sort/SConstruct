env = Environment(CPPPATH='#include', CCFLAGS='-g -Wall')

env.Program('tester', 'tester.c')

env.Program('bublsort', ['driver.c', 'src/bubblesort.c', 'src/swap.c'])
env.Program('inssort', ['driver.c', 'src/insertionsort.c', 'src/swap.c'])
env.Program('selsort', ['driver.c', 'src/selectionsort.c', 'src/swap.c'])
env.Program('heapsort', ['driver.c', 'src/heapsort.c', 'src/swap.c'])
env.Program('qsort', ['driver.c', 'src/quicksort.c', 'src/swap.c'])
env.Program('mergsort', ['driver.c', 'src/mergesort.c', 'src/swap.c'])
env.Program('shlsort', ['driver.c', 'src/shellsort.c', 'src/swap.c'])
env.Program('toursort', ['driver.c', 'src/tournamentsort.c', 'src/swap.c'])
env.Program('treesort', ['driver.c', 'src/treesort.c', 'src/swap.c'])
env.Program('bmsdsort', ['intsort.c', 'src/binarymsdsort.c'])
env.Program('lsdsort', ['intsort.c', 'src/lsdsort.c'])